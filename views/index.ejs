

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Users</title>
</head>
<body>
  <h1>All Users</h1>
  <ul>
    <% users.forEach(user => { %>
      <li><%= user._id %></li>
      <li><%= user.id %></li>
      <li><%= user.name %></li>
      <br>
    <% }); %>
  </ul>
</body>
</html>
